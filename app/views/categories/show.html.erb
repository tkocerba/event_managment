<%= title @category.title  %>
<% at_page %>

<div class="row">
  <div class="col-2 offset-10">
    <%= link_to 'Add Event', new_category_event_path(@category), class: 'btn btn-primary btn-sm mb-5 float-right'%>
  </div>
</div>

<p class="lead">
  <%= @category.description %>
</p>

<h2>Events</h2>
<%= render @events %>